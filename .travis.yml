# Use Docker inrfastructure
sudo: false

language: ruby
cache: bundler

rvm:
  - 2.3.1
  - 2.2.2

before_install:
  - gem install rubocop
  - gem install puppet-lint
  - bundle exec puppet-lint --no-autoloader_layout-check .
 
script:
  - bundle exec rubocop
